import React from "react";
import { Link } from "react-router-dom";

const Home = () => {
  return (
    <header className="hero-text">
      <h1>Welcome to My App</h1>
      <p>
        This is the home page of the app where you can navigate to different
        sections
      </p>
    </header>
  );
};

export default Home;
